export zeilen=0
echo Zunächst in der Shell Nr. $BASHPID
cat textdatei | while read zeile
do
    echo jetzt in der Subshell Nr. $BASHPID
    echo $zeile
    let zeilen=zeilen+1
    echo $zeilen
done
echo Wieder in Shell Nr. $BASHPID
echo $zeilen gelesen
